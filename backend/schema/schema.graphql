schema {
  query: Query
}

type Query {
  helloWorld(): String!
  getHistory(): [Commit]
}

type Commit {
  hash: String!
  message: String!
  date: String!
  author: Signature!
}

type Signature {
  name: String!
  email: String!
}